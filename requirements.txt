# Web framework
Flask==2.3.3

# PyTorch CPU-only (so it builds everywhere, no CUDA issues)
torch==2.8.0+cpu
-f https://download.pytorch.org/whl/torch_stable.html

# Transformers for KenBot AI
transformers==4.35.0
tokenizers==0.13.3  # prebuilt wheel, avoids Rust compile

# Text-to-Speech
pyttsx3==2.90

# Optional utilities
requests==2.32.5
numpy==2.3.3
